[mysqld]
# 基礎設定
server-id         = 223344
log_bin           = mysql-bin
binlog_format     = ROW
binlog_row_image  = FULL
expire_logs_days  = 7

# 這是針對 Debezium 的額外優化
binlog_rows_query_log_events = ON